# skill-glm-delegation-with-brief

Delegate mechanical work with a 4-part brief after the plan passed `orchestrator-consultant-gate`.

```bash
npx skills add dalsoop/skill-glm-delegation-with-brief -g -y
python3 eval/run.py
```

`npx` copies **files**. The `glm` CLI is host-private. If it is missing, write the brief and run the mechanical agent you have. Do not invent the plan in the child.

```bash
npx skills add dalsoop/orchestrator-consultant-gate -g -y
npx skills add dalsoop/skill-multi-model-overconfidence-guard -g -y
```

[`SKILL.md`](SKILL.md) · [`templates/`](templates/) · [`eval/`](eval/) · MIT

[![skills.sh](https://skills.sh/b/dalsoop/skill-glm-delegation-with-brief)](https://skills.sh/dalsoop/skill-glm-delegation-with-brief)
